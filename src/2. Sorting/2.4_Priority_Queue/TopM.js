import { MinPQ } from './MinPQ.js';
import { StdIn, StdOut } from "../../libs/index.js";
export class TopM {
    static main() {
        let pq;
        let m;
        StdOut.println('How many max record?')
        StdIn.read()
            .on('line', (line) => {
                const lineSplit = line.split(/\s+/);
                const a = parseInt(lineSplit[0]);

            })
            
    }

}